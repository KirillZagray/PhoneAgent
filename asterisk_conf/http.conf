; HTTP-сервер Asterisk — нужен для ARI (REST + WebSocket поверх него).
; Наружу не публикуется (порт 8088 не в docker-compose ports) — достучаться
; может только PhoneAgent-контейнер внутри compose-сети.

[general]
enabled=yes
bindaddr=0.0.0.0
bindport=8088
